# 技术规范

> 版本: v0.1 | 更新: 2026-06-25

---

## 一、整体架构（双路径设计）

本系统支持**两条并行路径**，共享同一套底层执行引擎：

```
用户输入（自然语言："创建一个三叶风扇"）
    │
    ├──[路径A]──────────────────────────────────────────────┐
    │  AI Agent 直达模式                                     │
    │  适合: 开发者 / 高级用户 / 批量建模                    │
    │  工具: Claude Desktop, Cursor, 任何 MCP Client         │
    │                                                        │
    │  用户 ↔ AI Agent (Claude)                              │
    │              │                                         │
    │              ├─ 理解意图、规划步骤                      │
    │              ├─ 调 MCP 工具 (nx_execute_plan)           │
    │              └─ 返回 .prt 文件路径                     │
    │                                                        │
    └──[路径B]──────────────────────────────────────────────┐
      Web 网页模式                                           │
      适合: 普通用户 / 不想接触命令行的用户                  │
      工具: 浏览器访问 http://localhost:3000                  │
                                                             │
      用户 → Web 前端 (Next.js + Three.js)                    │
                  │                                          │
                  ├─ 输入框 / 历史记录 / 3D 预览             │
                  ├─ HTTP POST → FastAPI 后端                  │
                  │                                          │
                  ▼                                          │
              FastAPI 后端                                    │
                  │                                          │
                  ├─ 调用 AI Agent API (DeepSeek/Claude)      │
                  │   → LLM 理解意图 + 生成建模计划           │
                  ├─ 执行建模 (MCP Server / 直接 executor)    │
                  │   → 生成 C# Journal → run_journal.exe     │
                  ├─ 返回结果                                 │
                  └─ Three.js 3D 预览                         │
```

**关键原则：两条路径共享同一套执行引擎。** 无论是 AI Agent 直接调用，还是 Web 端触发，最终都走同一条路：

```
自然语言 → AI 理解 → 结构化步骤 → C# Journal → run_journal.exe → NX → .prt
```

---

## 二、技术栈总览

```
┌─────────────────────────────────────────────────────────────────────┐
│  第1层: 前端 (Frontend)                                              │
│  ─────────────────────                                              │
│  框架: Next.js 14 (TypeScript)                                      │
│  3D渲染: React Three Fiber (@react-three/fiber)                     │
│           @react-three/drei, three-bvh-csg                          │
│  样式: Tailwind CSS                                                 │
│  通信: HTTP (fetch → FastAPI)                                       │
│                                                                     │
│  路径A: 不需要前端（直接连接 AI Agent）                              │
│  路径B: 浏览器访问 Web 界面                                          │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ HTTP
┌─────────────────────────────────────────────────────────────────────┐
│  第2层: 后端 API (Backend API)                                       │
│  ───────────────────────────                                          │
│  框架: FastAPI + Uvicorn                                             │
│  语言: Python 3.10+                                                  │
│  认证: CORS (开发阶段 localhost:3000)                                │
│                                                                     │
│  职责:                                                              │
│  - REST 路由 (/api/command, /api/session, etc.)                     │
│  - 会话管理 (SessionManager)                                         │
│  - 调用 AI Agent API (LLM 解析)                                      │
│  - 调用 MCP Server 执行建模                                          │
│  - 日志记录                                                          │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ 内部调用
┌─────────────────────────────────────────────────────────────────────┐
│  第3层: AI Agent 层                                                  │
│  ───────────────────                                                  │
│  LLM 引擎: DeepSeek API / Claude API                                 │
│  协议: langchain 或直接 HTTP API                                     │
│                                                                     │
│  职责:                                                              │
│  - 意图识别: 用户想"创建"还是"修改"还是"查询"                        │
│  - 结构分解: 三叶风扇 = 中心圆柱 + 3片叶子绕一圈                      │
│  - 步骤规划: 生成有序的建模步骤列表                                   │
│  - 参数填充: 补全默认尺寸和位置                                       │
│                                                                     │
│  路径A: 由 Claude Desktop 等外部 AI Agent 完成                       │
│  路径B: 由后端集成的 LLM API 完成                                    │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ 结构化步骤 (JSON)
┌─────────────────────────────────────────────────────────────────────┐
│  第4层: MCP Server / 执行引擎                                        │
│  ─────────────────────────                                            │
│  协议: MCP (Model Context Protocol) via stdio                        │
│  语言: Python 3.10+                                                  │
│  核心包: mcp>=1.0.0                                                  │
│                                                                     │
│  ├─ 工具注册: @mcp_tool 装饰器 + ToolRegistry                        │
│  ├─ 工具发现: pkgutil.iter_modules 自动扫描 tools/                   │
│  ├─ 参数校验: 从装饰器定义生成 JSON Schema                           │
│  ├─ 执行调度: 接收 tool_calls → JournalGenerator → .cs               │
│  └─ 进程管理: subprocess → run_journal.exe                           │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ 生成 C# 代码
┌─────────────────────────────────────────────────────────────────────┐
│  第5层: C# Journal 生成 (Code Generation)                            │
│  ───────────────────────────────────                                  │
│  生成器: backend/journal/generator.py                                │
│  封装库: backend/journal/nx_modeler.cs                               │
│  C# 版本: C# 5.0 (run_journal.exe 限制)                             │
│  编码: UTF-8 BOM (NX 编译器要求)                                     │
│                                                                     │
│  generator.py: JSON步骤 → 拼装 C# 方法调用字符串                      │
│  nx_modeler.cs: 实际的 NXOpen API 调用封装                            │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ subprocess
┌─────────────────────────────────────────────────────────────────────┐
│  第6层: NX 执行层 (Execution)                                        │
│  ──────────────────────────                                           │
│  执行器: run_journal.exe (NX 官方 C# 脚本执行器)                     │
│  CAD 引擎: Siemens NX 2412                                           │
│  输入: .cs 文件 (C# Journal 脚本)                                    │
│  输出: .prt 文件 (3D 模型)                                           │
│                                                                     │
│  生命周期: subprocess 启动 → 执行 → 退出 (不用一直开着)              │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 三、双路径详细数据流

### 路径A：AI Agent 直达模式

```
用户 (命令行/Claude Desktop)
  │  "给我创建一个三叶风扇"
  ▼
Claude Desktop (AI Agent)
  │  1. 理解用户意图
  │  2. 规划建模步骤
  │  3. 调用 MCP 工具 nx_execute_plan
  │
  ├── MCP Protocol (stdio) ──→ MCP Server
  │                               │
  │                               ├── 接收步骤列表
  │                               ├── JournalGenerator.generate()
  │                               ├── 保存 .cs 文件
  │                               ├── subprocess → run_journal.exe
  │                               ├── NX 执行 → 输出 .prt
  │                               └── 返回 .prt 路径
  │
  └── 用户收到: 模型已创建，文件在 output/models/xxx.prt
```

**特点：**
- 用户直接和 AI Agent 对话，体验最自然
- 适合开发者、批量建模、CI/CD 集成
- 需要用户自行配置 MCP Client（如 Claude Desktop）

### 路径B：Web 网页模式

```
用户 (浏览器)
  │  "创建一个三叶风扇"
  ▼
Web 前端 (Next.js + Three.js)
  │  POST /api/command { description: "三叶风扇" }
  ▼
FastAPI 后端
  │
  ├── 1. 调用 AI Agent API (DeepSeek/Claude)
  │      → 意图识别: 创建
  │      → 结构分解: 中心圆柱 + 叶片阵列
  │      → 步骤规划: cylinder → sketch → extrude → pattern_circular
  │
  ├── 2. 执行建模
  │      → 调用 MCP Server (内部 subprocess/直接调用)
  │      → JournalGenerator.generate(plan_steps)
  │      → run_journal.exe → .prt
  │
  ├── 3. 返回结果
  │      {
  │        status: "success",
  │        features: [{type: "cylinder", ...}, {type: "block", ...}],
  │        part_path: "output/models/xxx.prt",
  │        human_thought: "这是一个三叶风扇，中间是圆柱..."
  │      }
  │
  └── Web 前端
      ├── Three.js 根据 features 渲染 3D 预览
      ├── 聊天框显示 AI 的思考过程
      └── 特征列表显示所有建模步骤
```

**特点：**
- 用户通过浏览器操作，无需任何配置
- 内置 3D 预览（Three.js），即时看到结果
- 同样经过 AI Agent 解析，智能程度不降级

---

## 四、各层详解

### 4.1 前端层 (Frontend)

```
技术选型:
  Next.js 14 + TypeScript     ← 应用框架
  React Three Fiber           ← 3D 渲染
  Tailwind CSS                ← 样式
  three-bvh-csg               ← 客户端布尔运算（预览用）

页面布局:
  ┌─────────────────────────────────────────────┐
  │  侧边栏 (会话历史)  │  3D预览区  │  AI 聊天  │
  │                    │  (Three.js) │ 输入框    │
  │  - 历史会话列表     │  视角控制    │ 消息记录  │
  │  - 特征列表        │  缩放/旋转  │ 思考过程  │
  └─────────────────────────────────────────────┘

3D 预览坐标映射:
  NX 坐标系: X=右, Y=前, Z=上
  Three.js:  X=右, Y=上, Z=前 (渲染时转换)

渲染模式:
  1. 独立特征渲染 (每个特征独立显示)
  2. CSG 布尔运算 (合并/减去/相交)
  3. 回退: CSG 失败时用独立模式
```

### 4.2 后端 API 层 (Backend API)

```
路由设计:
  POST   /api/command          执行建模指令
  POST   /api/session/new      创建新会话
  GET    /api/sessions         获取会话列表
  GET    /api/session/{id}     获取会话详情

核心处理流程 (路径B):
  POST /api/command
  1. 解析请求 { description, session_id }
  2. 调用 AI Agent API 解析自然语言 → 结构化步骤
  3. 通过执行引擎建模
  4. 返回特征列表 + 模型路径 + AI 思考过程

AI Agent 调用:
  LLM: DeepSeek / Claude API
  输入: 用户的自然语言描述
  输出: 结构化建模步骤
  prompt: 包含所有可用工具的 JSON Schema 描述
```

### 4.3 AI Agent 层

```
路径A: 外部 AI Agent (Claude Desktop)
  - 用户自行配置 MCP 连接
  - AI Agent 直接理解、规划、调用 MCP 工具
  - 不经过本系统的后端

路径B: 内部 AI Agent 集成
  - 通过 FastAPI 后端调用 LLM API
  - Agent Pipeline 包含:
    1. 意图识别 (CREATE / MODIFY / QUERY / DELETE / ANALYZE / CHAT)
    2. 结构分解 (阵列/镜像/旋转体/堆叠等7种策略)
    3. 步骤规划 (确定先后顺序和参数)
    4. 参数补全 (LLM 补全默认值)
  - 输出: 结构化步骤列表 → 执行引擎

两种路径的 AI 理解能力应当保持一致。
```

### 4.4 MCP 协议层 (MCP Server)

```
传输方式: stdio (标准输入输出)
协议版本: MCP 1.0+

核心机制:
  工具注册:
    @mcp_tool(name, description, params)
    async def nx_execute_plan(steps) -> ToolResult
      ↓
    ToolRegistry.register(_ToolDef)
      ↓
    server.list_tools() → 返回所有工具的 JSON Schema

  工具调用:
    server.call_tool() → handler(**arguments)
      ↓
    handler 内部:
      1. 构建步骤列表
      2. JournalGenerator.generate(steps) → C# 代码
      3. 保存 .cs 文件
      4. subprocess → run_journal.exe
      5. 返回 ToolResult

  工具列表 (将逐步扩展):
    ⭐ nx_execute_plan   核心: 执行一组建模步骤
    nx_validate_plan     验证步骤合法性
    nx_create_block      创建方块
    nx_create_cylinder   创建圆柱
    nx_create_cone       创建圆锥
    nx_create_sphere     创建球体
    nx_create_torus      创建圆环
    ... (更多工具)
```

### 4.5 C# 执行层 (Journal Generator + nx_modeler.cs)

```
JSON → C# 转换:
  输入:
    [{"tool": "block", "params": {"length": 100, "width": 80, "height": 60}}]

  生成器: generator.py → 调用 _gen_block(params)
    → 返回字符串: 'm.Tool_Block(0.0, 0.0, 0.0, 100.0, 80.0, 60.0);'

  完整脚本结构:
    using System;
    using System.Collections.Generic;
    using NXOpen;
    using NXOpen.Features;
    using NXOpen.GeometricUtilities;

    public class NXJournal {
      public static void Main(string[] args) {
        NXModeler m = new NXModeler();
        m.Init();
        m.Tool_Block(...);   // ← 生成的调用
        m.Tool_SaveModel(...);
      }
    }

    // nx_modeler.cs 的内容（实际的 NXOpen API 实现）
    public class NXModeler {
      public void Tool_Block(double x, double y, double z, double l, double w, double h) {
        // NXOpen API 调用
      }
    }

  nx_modeler.cs 约束:
    - C# 5.0 语法（run_journal.exe 的编译器限制）
    - 不支持 $"" 字符串插值
    - 文件保存为 UTF-8 BOM 编码
```

### 4.6 NX 执行层 (run_journal.exe)

```
执行方式:
  subprocess.run(
    [run_journal.exe, journal.cs],
    capture_output=True,
    timeout=180,
    encoding="gbk"
  )

生命周期:
  启动 → 编译 C# → 连接 NX License → 执行建模 → 保存 .prt → 退出
  (整个过程是一个独立进程，不占用 NX 主程序)

输入:
  .cs 文件 (C# Journal 脚本)

输出:
  .prt 文件 (NX 3D 模型)
  控制台输出 (建模日志)

路径配置:
  NX_ROOT=C:\Program Files\Siemens\NX
  RUN_JOURNAL_EXE=%NX_ROOT%\NXBIN\run_journal.exe
```

---

## 五、层间接口规范

### 5.1 前端 → 后端 (HTTP)

```json
POST /api/command
Request:
{
  "session_id": "abc123",        // 可选，不传则创建新会话
  "description": "三叶风扇"      // 用户输入
}

Response:
{
  "status": "success",
  "session_id": "abc123",
  "message": "模型已生成",
  "features": [
    {
      "feature_id": "feat_001",
      "type": "cylinder",
      "parameters": {"diameter": 60, "height": 20},
      "position": {"x": 0, "y": 0, "z": 0},
      "boolean_op": "create"
    }
  ],
  "part_path": "output/models/xxx.prt",
  "human_thought": "这是一个三叶风扇...",
  "steps_count": 5
}
```

### 5.2 后端 → MCP Server (内部调用)

```python
# 直接调用 executor (不经过 stdio, 减少协议开销)
from backend.mcp.executor import execute_journal
from backend.journal.generator import JournalGenerator

steps = [
    {"tool": "cylinder", "params": {"radius": 30, "height": 20}},
    {"tool": "create_sketch_on_plane", "params": {}},
    {"tool": "draw_rectangle", "params": {...}},
]
gen = JournalGenerator(tag="fan")
code = gen.generate(steps)
result = execute_journal(code, tag="fan")
```

### 5.3 MCP Server → AI Agent (MCP 协议)

```
AI Agent (Claude Desktop)                  MCP Server
       │                                        │
       │──── JSON-RPC (list_tools) ────────────→│
       │←─── Tool[] (JSON Schema) ─────────────│
       │                                        │
       │──── JSON-RPC (call_tool, nx_execute) ─→│
       │←─── TextContent (执行结果) ────────────│
```

### 5.4 后端 → NX (subprocess)

```
subprocess.run(
  [run_journal.exe, "path/to/journal.cs"],
  capture_output=True,
  timeout=180,
  encoding="gbk"
)
```

---

## 六、目录结构

```
Lang-to-NX-MCP/
│
├── run.py                          # 启动入口 (MCP Server / FastAPI)
├── requirements.txt                # Python 依赖
├── .env                            # 环境配置
├── .env.example                    # 环境配置示例
│
├── backend/                        # 后端核心
│   ├── __init__.py
│   │
│   ├── config.py                   # 配置管理 (加载 .env)
│   │
│   ├── api.py                      # FastAPI 路由 (Web 模式)
│   ├── session_manager.py          # 会话管理
│   ├── log_system.py               # 日志系统
│   │
│   ├── mcp/                        # ★ MCP 协议层
│   │   ├── server.py               # MCP Server (stdio 传输)
│   │   ├── registry.py             # @mcp_tool 装饰器 + ToolRegistry
│   │   ├── response.py             # ToolResult / ToolError
│   │   ├── executor.py             # 执行引擎 (生成 C# + subprocess)
│   │   └── tools/                  # MCP 工具实现
│   │       ├── execute.py          # nx_execute_plan (核心)
│   │       └── primitives.py       # 便捷工具 (block/cylinder...)
│   │
│   ├── agent/                      # ★ AI Agent 管线 (路径B用)
│   │   ├── intent.py               # 意图识别
│   │   ├── decomposer.py           # 结构分解
│   │   ├── memory.py               # 上下文记忆
│   │   ├── planner.py              # 步骤规划
│   │   └── executor.py             # AgentPipeline 编排
│   │
│   └── journal/                    # ★ C# 执行层
│       ├── generator.py            # JSON → C# 代码生成
│       └── nx_modeler.cs           # NXOpen C# API 封装
│
├── frontend/                       # ★ 前端 (Next.js)
│   ├── app/
│   │   ├── page.tsx                # 主页面
│   │   ├── types.ts                # TypeScript 类型
│   │   ├── globals.css             # 样式
│   │   └── components/
│   │       ├── ThreeViewer.tsx     # 3D 预览 (Three.js)
│   │       └── ViewControls.tsx    # 视角控制
│   └── package.json
│
├── output/                         # 输出目录
│   ├── models/                     # .prt 模型文件
│   └── scripts/                    # .cs Journal 脚本
│
├── logs/                           # 日志
├── docs/                           # 文档
│
└── tests/                          # 测试
```

---

## 七、关键设计决策记录

### 7.1 为什么用 C# Journal + run_journal.exe，而不是 Python NXOpen API？

```
❌ Python NXOpen API（进程内）:
  优点: 响应快、可交互
  缺点:
    - NX 必须一直打开（占用 License）
    - NX 进程内跑 Python 重型逻辑不稳定
    - 不适合批量建模

✅ C# Journal + run_journal.exe（进程外）:
  优点:
    - NX 用完即退，不占 License
    - 进程隔离，崩了不影响主系统
    - 适合批量/自动化场景
  缺点:
    - 每次执行有进程启动开销
    - C# 5.0 编译器限制（不支持 $"" 等语法）
    - UTF-8 路径需要特殊处理
```

### 7.2 为什么同时支持 AI Agent 和 Web 两种路径？

```
两种模式覆盖不同用户群:

  AI Agent 模式 (路径A):
    - 面向: 开发者、技术用户、批量模型生成
    - 体验: 命令行对话，直接
    - 优势: 灵活、可编程、可集成到 CI/CD

  Web 模式 (路径B):
    - 面向: 普通用户、非技术用户
    - 体验: 图形界面，直观
    - 优势: 零配置、3D 预览、历史记录

  两者不互斥，共享底层引擎。
  Web 模式的后端同样调用 AI Agent API 进行自然语言理解。
```

### 7.3 为什么 MCP 工具 handler 不走 MCP 协议而是直接调用 executor？

```
路径B (Web → FastAPI → 建模) 需要执行效率。
如果 Web 请求也要经过完整的 MCP stdio 协议，会多一层序列化开销。

因此:
  - 路径A (外部 AI Agent): 通过 MCP stdio 协议调用
  - 路径B (Web): FastAPI 后端直接调用 executor.py 的 Python API

两种路径最终执行的是同一套 JournalGenerator + execute_journal。
```

---

## 八、命名规范

| 概念 | 规则 | 示例 |
|------|------|------|
| MCP 工具名 | `nx_` 前缀 + 小写蛇形 | `nx_execute_plan`, `nx_create_block` |
| 工具模块 | 小写蛇形 | `primitives.py`, `execute.py` |
| C# 方法 | `Tool_` 前缀 + 帕斯卡 | `Tool_Block`, `Tool_Extrude` |
| generator 方法 | `_gen_` 前缀 + 小写蛇形 | `_gen_block`, `_gen_extrude` |
| JSON 参数名 | 小写蛇形 | `major_radius`, `feature_ref` |
| 文件命名 | ASCII 字符 + 时间戳 | `2026_06_25_3Yfan_0.cs` |
| 环境变量 | 大写蛇形 | `NX_ROOT`, `RUN_JOURNAL_EXE` |

---

## 九、开发路线图

```
Phase 1: 基础架构 ✅ (已完成)
  - MCP 协议层搭建
  - Journal 生成器
  - nx_modeler.cs 继承
  - 核心工具 (nx_execute_plan + 基本体素)

Phase 2: 修复 C# 执行层 🚧 (当前阶段)
  - 修复 Revolve crash (P0)
  - 修复 Translate/Rotate 错误 (P0)
  - 补全 A/C 类 TODO 工具
  - 补全 B 类 TODO 工具

Phase 3: 后端 API + Web 前端
  - FastAPI 路由
  - AI Agent 集成 (LLM API)
  - 会话管理
  - 前端 Three.js 预览

Phase 4: 增强
  - Agent Pipeline 移植 (intent → decomposer → planner)
  - 错误自动修正
  - 多轮对话
  - 测试套件
```

---

*参见: [01-项目介绍.md](./01-项目介绍.md), [02-现状与问题.md](./02-现状与问题.md)*
